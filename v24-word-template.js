(()=>{
  const TEMPLATE_URL='assets/word-template-v24.docx?v=24';
  const PIZZIP_URLS=['https://cdn.jsdelivr.net/npm/pizzip@3.2.0/dist/pizzip.min.js','https://unpkg.com/pizzip@3.2.0/dist/pizzip.min.js','https://cdnjs.cloudflare.com/ajax/libs/pizzip/3.2.0/pizzip.min.js'];
  const W='http://schemas.openxmlformats.org/wordprocessingml/2006/main';
  const clean=v=>String(v??'').trim();
  const normalize=v=>clean(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\s+/g,' ');
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const value=(...ids)=>{for(const id of ids){const e=document.getElementById(id);if(e&&clean(e.value))return clean(e.value)}return ''};
  const radio=n=>clean($(`input[name="${n}"]:checked`)?.value);
  const safe=v=>clean(v)||'Neuvedeno';
  const escapeXml=v=>clean(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');
  const people=id=>{const c=document.getElementById(id);return c?$$('.repeat-row',c).map(r=>({name:clean($('[data-row-field="name"]',r)?.value),personalNumber:clean($('[data-row-field="personalNumber"]',r)?.value)})).filter(p=>p.name||p.personalNumber):[]};
  const signatureData=()=>{const mode=normalize(radio('signatureMode'));if(mode.includes('print')||mode.includes('tisk'))return '';const c=$('#signatureCanvas')||$('#sigCanvas')||$('canvas[id*="signature" i]')||$('canvas[id*="sig" i]');try{return c?.toDataURL('image/png')||''}catch(_){return ''}};
  const collect=()=>{
    const bp=value('bankPrefix'),bn=value('bankAccountNumber','bankNumber'),bc=value('bankCode');
    const account=[bp&&bn?`${bp}-${bn}`:bn,bc].filter(Boolean).join('/');
    const educationLevel=value('educationLevel')==='__other__'?value('educationLevelOther'):value('educationLevel');
    const education=[educationLevel,value('educationMajor')].filter(Boolean).join('; ')||value('education');
    const birthPlace=[value('birthPlaceCity'),value('birthDistrict')?`okres ${value('birthDistrict').replace(/^okres\s+/i,'')}`:'',value('birthCountry')].filter(Boolean).join('; ')||value('birthPlace');
    const pc=clean($('#phonePrefixChoice')?.value||'');
    const prefix=value('phonePrefixCustom')||(pc.includes('|')?pc.split('|')[1]:pc.match(/\+\d+/)?.[0]||'');
    return {fullName:value('fullName'),previousSurname:value('previousSurname'),birthSurname:value('birthSurname'),maritalStatus:value('maritalStatusOther')||value('maritalStatus'),citizenship:value('citizenshipOther')||value('citizenship'),healthInsurance:value('healthInsurance'),personalNumber:value('personalNumber'),birthDate:value('birthDate'),birthPlace,permanentAddress:value('permanentAddress'),permanentPostal:value('permanentPostal'),phone:[prefix,value('phoneNational')].filter(Boolean).join(' '),correspondenceAddress:value('correspondenceAddress'),correspondencePostal:value('correspondencePostal'),dataBox:value('dataBox'),email:value('email'),bankAccount:account,spouseName:value('spouseName'),spousePersonalNumber:value('spousePersonalNumber'),children:people('childrenRows'),dependents:people('dependentRows'),education,educationYear:value('educationYear'),drivingLicence:radio('drivingLicence'),executionInsolvency:radio('executionInsolvency'),closePersonEmployed:radio('closePersonEmployed'),closePersonDetails:value('closePersonDetails'),otherActivity:radio('otherActivity'),activityType:value('activityType'),pension:radio('pension'),pensionType:value('pensionType'),pensionGrantedDate:value('pensionGrantedDate'),disability:radio('disability'),disabilityType:value('disabilityType'),disabilityDecisionDate:value('disabilityDecisionDate'),signaturePlace:value('signaturePlace'),signatureDate:value('signatureDate'),signatureData:signatureData()};
  };
  const insuranceCode=v=>clean(v).match(/(?:^|\D)(\d{3})(?:\D|$)/)?.[1]||'';
  const digits=v=>clean(v).replace(/\D/g,'').slice(0,10).split('');
  const valuesFor=d=>{
    const children=d.children.length<=3?[...d.children]:[d.children[0],d.children[1],{name:d.children.slice(2).map(p=>[p.name,p.personalNumber&&`(${p.personalNumber})`].filter(Boolean).join(' ')).join('; '),personalNumber:''}];while(children.length<3)children.push({name:'',personalNumber:''});
    const one=d.dependents.length===1?d.dependents[0]:null;
    const dep=d.dependents.length>1?d.dependents.map(p=>[p.name||'Neuvedeno',p.personalNumber&&`(${p.personalNumber})`].filter(Boolean).join(' ')).join('; '):(one?.name||'Neuvedeno');
    const v={FULL_NAME:safe(d.fullName),PREVIOUS_SURNAME:safe(d.previousSurname),BIRTH_SURNAME:safe(d.birthSurname),MARITAL_STATUS:safe(d.maritalStatus),CITIZENSHIP:safe(d.citizenship),HEALTH_INSURANCE:insuranceCode(d.healthInsurance)||safe(d.healthInsurance),BIRTH_DATE:safe(d.birthDate),BIRTH_PLACE:safe(d.birthPlace),PERMANENT_ADDRESS:safe(d.permanentAddress),PERMANENT_POSTAL:safe(d.permanentPostal),PHONE:safe(d.phone),CORRESPONDENCE_ADDRESS:safe(d.correspondenceAddress),CORRESPONDENCE_POSTAL:safe(d.correspondencePostal),DATA_BOX:safe(d.dataBox),EMAIL:safe(d.email),BANK_ACCOUNT:safe(d.bankAccount),SPOUSE_NAME:safe(d.spouseName),CHILD1_NAME:safe(children[0].name),CHILD2_NAME:safe(children[1].name),CHILD3_NAME:safe(children[2].name),DEPENDENTS:dep,EDUCATION:safe(d.education),EDUCATION_YEAR:safe(d.educationYear),DRIVING_LICENCE:safe(d.drivingLicence),EXECUTION:safe(d.executionInsolvency),CLOSE_PERSON:safe(d.closePersonEmployed),CLOSE_PERSON_DETAILS:safe(d.closePersonDetails),OTHER_ACTIVITY:safe(d.otherActivity),ACTIVITY_TYPE:safe(d.activityType),PENSION:safe(d.pension),PENSION_TYPE:safe(d.pensionType),PENSION_DATE:safe(d.pensionGrantedDate),DISABILITY:safe(d.disability),DISABILITY_TYPE:safe(d.disabilityType),DISABILITY_DATE:safe(d.disabilityDecisionDate),SIGN_PLACE:safe(d.signaturePlace),SIGN_DATE:safe(d.signatureDate)};
    const add=(p,raw)=>{const a=digits(raw);for(let i=1;i<=10;i++)v[p+i]=a[i-1]||''};
    add('PERSONAL_RC_',d.personalNumber);add('SPOUSE_RC_',d.spousePersonalNumber);add('CHILD1_RC_',children[0].personalNumber);add('CHILD2_RC_',children[1].personalNumber);add('CHILD3_RC_',children[2].personalNumber);add('DEPENDENT_RC_',one?.personalNumber||'');return v;
  };
  const loadScript=src=>new Promise((ok,no)=>{const s=document.createElement('script');s.src=src;s.async=true;s.crossOrigin='anonymous';s.onload=ok;s.onerror=()=>{s.remove();no(new Error(`Nelze načíst ${src}`))};document.head.appendChild(s)});
  const ensureZip=async()=>{if(window.PizZip)return window.PizZip;let err;for(const src of PIZZIP_URLS){try{await loadScript(src);if(window.PizZip)return window.PizZip}catch(e){err=e}}throw err||new Error('Knihovna pro Word se nenačetla.')};
  let templatePromise;
  const loadTemplate=()=>templatePromise||(templatePromise=fetch(TEMPLATE_URL,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error(`Word šablona: HTTP ${r.status}`);return r.arrayBuffer()}).then(buffer=>new Uint8Array(buffer)));
  const printSignature=xml=>{const doc=new DOMParser().parseFromString(xml,'application/xml');if(doc.querySelector('parsererror'))throw new Error('Podpisovou část Wordu se nepodařilo zpracovat.');const p=[...doc.getElementsByTagNameNS(W,'p')].reverse().find(x=>normalize(x.textContent).includes('podpis'));if(!p)return xml;const run=[...p.children].find(r=>r.localName==='r'&&r.getElementsByTagNameNS(W,'drawing').length);if(!run)return xml;const nr=doc.createElementNS(W,'w:r'),rp=[...run.children].find(x=>x.localName==='rPr');if(rp)nr.appendChild(rp.cloneNode(true));const t=doc.createElementNS(W,'w:t');t.textContent='……………………………….';nr.appendChild(t);p.replaceChild(nr,run);return new XMLSerializer().serializeToString(doc)};
  const generate=async()=>{const PizZip=await ensureZip(),zip=new PizZip(await loadTemplate()),d=collect();if(!d.fullName)throw new Error('Doplňte jméno a příjmení.');const replacements=valuesFor(d);Object.keys(zip.files).filter(n=>/\.xml$|\.rels$/.test(n)).forEach(n=>{const f=zip.file(n);if(!f)return;let xml=f.asText();for(const [k,v] of Object.entries(replacements))xml=xml.split(`@@${k}@@`).join(escapeXml(v));zip.file(n,xml)});if(d.signatureData){const b=d.signatureData.split(',')[1]||'';if(b)zip.file('word/media/image1.png',b,{base64:true})}else{const f=zip.file('word/document.xml');if(f)zip.file('word/document.xml',printSignature(f.asText()))}const blob=zip.generate({type:'blob',compression:'DEFLATE',mimeType:'application/vnd.openxmlformats-officedocument.wordprocessingml.document'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`Osobni_dotaznik_${d.fullName.replace(/[^\p{L}\p{N}]+/gu,'_').replace(/^_+|_+$/g,'')||'zamestnanec'}.docx`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),5000)};
  const removePdf=()=>{['createPdf','generatePdf','previewPdf','downloadPdf','printPdf'].forEach(id=>document.getElementById(id)?.remove());$$('button,a').forEach(e=>{const t=normalize(e.textContent);if(t.includes('pdf')||t.includes('vytisknout'))e.remove()})};
  const install=()=>{removePdf();const old=document.getElementById('generateWordV23')||document.getElementById('generateWordV12')||$$('button,a').find(e=>normalize(e.textContent).includes('vygenerovat word'));if(!old)return false;const b=old.cloneNode(true);b.id='generateWordV24';b.type='button';b.textContent='Vygenerovat Word';old.replaceWith(b);const status=document.querySelector('.v12-output-status');b.addEventListener('click',async e=>{e.preventDefault();e.stopImmediatePropagation();b.disabled=true;b.textContent='Vytvářím Word…';if(status){status.textContent='Vytvářím Word…';status.style.color='#00573f'}try{await generate();if(status){status.textContent='Word byl vytvořen a stažen.';status.style.color='#00573f'}}catch(err){console.error('Word v24:',err);if(status){status.textContent=err.message||'Word se nepodařilo vytvořit.';status.style.color='#c5221f'}}finally{b.disabled=false;b.textContent='Vygenerovat Word'}},true);loadTemplate().catch(e=>console.error('Načtení šablony v24:',e));return true};
  const start=Date.now(),timer=setInterval(()=>{if(install())clearInterval(timer);else if(Date.now()-start>30000)clearInterval(timer)},50);
})();
